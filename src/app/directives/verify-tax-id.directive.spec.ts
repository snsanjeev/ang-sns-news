import { VerifyTaxIdDirective } from './verify-tax-id.directive';

describe('VerifyTaxIdDirective', () => {
  it('should create an instance', () => {
    const directive = new VerifyTaxIdDirective();
    expect(directive).toBeTruthy();
  });
});
