import { TaxidFormatDirective } from './taxid-format.directive';

describe('TaxidFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new TaxidFormatDirective();
    expect(directive).toBeTruthy();
  });
});
